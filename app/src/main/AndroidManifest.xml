<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="me.leoyuu.opengl">


    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:required="true" android:glEsVersion="0x00030000"/>
    <supports-gl-texture android:name="GL_OES_compressed_ETC1_RGB8_texture" />
    <supports-gl-texture android:name="GL_OES_compressed_paletted_texture" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:name=".App"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".nrenders.GlActivity" />
        <activity android:name=".vr.VrActivity" />
        <activity android:name=".ctrl.jump.JumpActivity" />
        <activity android:name=".camera.CameraActivity" />
        <activity android:name=".img.ImgActivity" />
        <activity android:name=".tmp.TmpActivity" />
        <activity android:name=".jni.JniActivity" />
        <activity android:name=".obj.ObjViewActivity" />
        <activity android:name=".esv3.V3Activity" />
    </application>

</manifest>